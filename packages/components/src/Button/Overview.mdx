import { Meta } from '@storybook/addon-docs/blocks';
import { Button } from './Button';
import { Button as MuiButton } from '../mui/Button';
import React from 'react';

<Meta title="Components/Button" />

<div className="sb-unstyled">
  <div className="docs-page-title">Button</div>
  <div className="docs-description">
    Buttons allow users to trigger actions within the interface. They are used to submit forms,
    confirm choices, or initiate processes. Available as a lightweight React component and as a
    themed Material UI component — both produce identical visuals using CKW design tokens.
  </div>
</div>

<div className="sb-unstyled">
  <div className="docs-section">
    <div className="docs-section-title">Visual Overview</div>
    <div className="docs-subsection-description">
      Three visual types communicate hierarchy. Both implementations render identically.
    </div>
  </div>
</div>

<div style={{ display: 'flex', flexDirection: 'column', gap: '48px', marginTop: '24px' }}>
  <div>
    <div className="sb-unstyled">
      <div
        style={{
          fontFamily: 'var(--font-family-brand), sans-serif',
          fontSize: '14px',
          fontWeight: 500,
          color: 'var(--text-secondary)',
          marginBottom: '16px',
        }}
      >
        React
      </div>
    </div>
    <div style={{ display: 'flex', gap: '16px', alignItems: 'center', flexWrap: 'wrap' }}>
      <Button type="Primary">Primary</Button>
      <Button type="Secondary">Secondary</Button>
      <Button type="Tertiary">Tertiary</Button>
    </div>
  </div>
  <div>
    <div className="sb-unstyled">
      <div
        style={{
          fontFamily: 'var(--font-family-brand), sans-serif',
          fontSize: '14px',
          fontWeight: 500,
          color: 'var(--text-secondary)',
          marginBottom: '16px',
        }}
      >
        MUI
      </div>
    </div>
    <div style={{ display: 'flex', gap: '16px', alignItems: 'center', flexWrap: 'wrap' }}>
      <MuiButton variant="Primary">Primary</MuiButton>
      <MuiButton variant="Secondary">Secondary</MuiButton>
      <MuiButton variant="Tertiary">Tertiary</MuiButton>
    </div>
  </div>
</div>

<div className="sb-unstyled">
  <div className="docs-section">
    <div className="docs-section-title">Choose Your Implementation</div>
    <div className="docs-subsection-description">
      Pick the implementation that fits your project. Both use the same design tokens and produce
      identical visual output.
    </div>
  </div>
</div>

<div
  className="sb-unstyled"
  style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '24px', marginTop: '24px' }}
>
  <div
    style={{
      padding: '24px',
      border: '1px solid var(--border-soft)',
      borderRadius: 'var(--border-radius-s)',
    }}
  >
    <div
      style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: '18px',
        fontWeight: 500,
        color: 'var(--text-primary)',
        marginBottom: '8px',
      }}
    >
      React
    </div>
    <div
      style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: '14px',
        color: 'var(--text-secondary)',
        lineHeight: 1.6,
      }}
    >
      Lightweight, zero-dependency component. CSS-only styling with design tokens. Best for projects
      that don't use MUI.
    </div>
  </div>
  <div
    style={{
      padding: '24px',
      border: '1px solid var(--border-soft)',
      borderRadius: 'var(--border-radius-s)',
    }}
  >
    <div
      style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: '18px',
        fontWeight: 500,
        color: 'var(--text-primary)',
        marginBottom: '8px',
      }}
    >
      MUI
    </div>
    <div
      style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: '14px',
        color: 'var(--text-secondary)',
        lineHeight: 1.6,
      }}
    >
      Material UI Button with CKW theme applied. Full MUI feature set (sx prop, slots, etc.). Best
      for projects already using MUI.
    </div>
  </div>
</div>

<div className="sb-unstyled">
  <div className="docs-section">
    <div className="docs-section-title">Anatomy</div>
    <div className="docs-subsection-description">
      Both implementations share the same anatomy and token usage.
    </div>
  </div>
</div>

<div className="sb-unstyled" style={{ marginTop: '16px' }}>
  <table className="docs-table">
    <thead>
      <tr>
        <th>Property</th>
        <th>Token</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Height (Large/Medium)</td>
        <td className="cell-mono">48px</td>
        <td>Fixed</td>
      </tr>
      <tr>
        <td>Height (Small)</td>
        <td className="cell-mono">40px</td>
        <td>Fixed</td>
      </tr>
      <tr>
        <td>Border radius</td>
        <td className="cell-mono">--border-radius-full</td>
        <td>99999px (pill)</td>
      </tr>
      <tr>
        <td>Font (Large)</td>
        <td className="cell-mono">--label-l-*</td>
        <td>Gotham Medium 18/22</td>
      </tr>
      <tr>
        <td>Font (Medium/Small)</td>
        <td className="cell-mono">--label-m-*</td>
        <td>Gotham Medium 16/20</td>
      </tr>
      <tr>
        <td>Primary background</td>
        <td className="cell-mono">--gradient-brand</td>
        <td>Linear gradient 210°</td>
      </tr>
      <tr>
        <td>Primary hover</td>
        <td className="cell-mono">--interactive-primary-hover</td>
        <td>Solid green-700</td>
      </tr>
    </tbody>
  </table>
</div>
