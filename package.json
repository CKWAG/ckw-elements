{
  "name": "ckw-elements",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "license": "Apache-2.0",
  "description": "CKW Elements Design System — Monorepo",
  "scripts": {
    "build": "pnpm -r run build",
    "changeset": "changeset",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm --filter @ckwag/elements-icons run build && tsc -p packages/components && tsc -p apps/storybook",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "release:build": "pnpm --filter @ckwag/elements-tokens run build && pnpm --filter @ckwag/elements-icons run build && pnpm --filter @ckwag/elements run build && pnpm --filter @ckwag/elements-storybook run build",
    "release:check": "node scripts/validate-release.mjs",
    "release:pack": "npm pack ./packages/tokens --dry-run --cache ./.npm-pack-cache && npm pack ./packages/icons --dry-run --cache ./.npm-pack-cache && npm pack ./packages/components --dry-run --cache ./.npm-pack-cache",
    "release:publish": "pnpm --filter @ckwag/elements-tokens run build && pnpm --filter @ckwag/elements-icons run build && pnpm --filter @ckwag/elements run build && node scripts/validate-release.mjs --publish && changeset publish",
    "release:validate": "pnpm format:check && pnpm typecheck && pnpm release:build && pnpm release:check && pnpm release:pack",
    "tokens:sync": "pnpm --filter @ckwag/elements-tokens run tokens:sync",
    "version:packages": "changeset version"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "style-dictionary",
      "@bundled-es-modules/glob"
    ],
    "overrides": {
      "picomatch": ">=4.0.4",
      "brace-expansion@^2": ">=2.0.3 <3",
      "postcss": ">=8.5.10"
    }
  },
  "packageManager": "pnpm@10.11.0",
  "lint-staged": {
    "*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.2",
    "prettier": "^3.8.3",
    "typescript": "^5.9.0"
  }
}
