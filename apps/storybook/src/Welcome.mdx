import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Welcome" />

<div className="sb-unstyled">

<div className="hero-banner">
  <img
    className="hero-banner__bg"
    src="/header_graphic_text.png"
    alt="CKW Elements – The CKW Design System for a harmonized and accessible user experience across digital touchpoints."
  />
</div>

<div className="docs-section" style={{ marginTop: '48px' }}>
  <div className="docs-section-title">Explore</div>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fill, minmax(220px, 1fr))',
    gap: 'var(--spacing-m)',
    marginTop: 'var(--spacing-l)',
  }}>

    <div style={{
      padding: 'var(--spacing-xl)',
      borderRadius: 'var(--border-radius-s)',
      border: '1px solid var(--border-soft)',
      background: 'var(--background-default)',
    }}>
      <div style={{
        fontSize: '28px',
        marginBottom: 'var(--spacing-xs)',
      }}>🔠</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--label-m-size)',
        fontWeight: 'var(--label-m-weight)',
        color: 'var(--text-primary)',
        marginBottom: 'var(--spacing-2xs)',
      }}>Typography</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--body-s-size)',
        fontWeight: 'var(--body-s-weight)',
        color: 'var(--text-secondary)',
        lineHeight: '1.4',
      }}>Gotham typeface, 28 styles across Display, Heading, Body, Caption, Label, and Metric.</div>
    </div>

    <div style={{
      padding: 'var(--spacing-xl)',
      borderRadius: 'var(--border-radius-s)',
      border: '1px solid var(--border-soft)',
      background: 'var(--background-default)',
    }}>
      <div style={{
        fontSize: '28px',
        marginBottom: 'var(--spacing-xs)',
      }}>🎨</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--label-m-size)',
        fontWeight: 'var(--label-m-weight)',
        color: 'var(--text-primary)',
        marginBottom: 'var(--spacing-2xs)',
      }}>Colors</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--body-s-size)',
        fontWeight: 'var(--body-s-weight)',
        color: 'var(--text-secondary)',
        lineHeight: '1.4',
      }}>79 primitives across 7 hues, 76 semantic tokens for surfaces, text, borders, and status.</div>
    </div>

    <div style={{
      padding: 'var(--spacing-xl)',
      borderRadius: 'var(--border-radius-s)',
      border: '1px solid var(--border-soft)',
      background: 'var(--background-default)',
    }}>
      <div style={{
        fontSize: '28px',
        marginBottom: 'var(--spacing-xs)',
      }}>📏</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--label-m-size)',
        fontWeight: 'var(--label-m-weight)',
        color: 'var(--text-primary)',
        marginBottom: 'var(--spacing-2xs)',
      }}>Spacings</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--body-s-size)',
        fontWeight: 'var(--body-s-weight)',
        color: 'var(--text-secondary)',
        lineHeight: '1.4',
      }}>15 spacing tokens from 0px to 120px for consistent layout rhythm.</div>
    </div>

    <div style={{
      padding: 'var(--spacing-xl)',
      borderRadius: 'var(--border-radius-s)',
      border: '1px solid var(--border-soft)',
      background: 'var(--background-default)',
    }}>
      <div style={{
        fontSize: '28px',
        marginBottom: 'var(--spacing-xs)',
      }}>🔲</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--label-m-size)',
        fontWeight: 'var(--label-m-weight)',
        color: 'var(--text-primary)',
        marginBottom: 'var(--spacing-2xs)',
      }}>Borders</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--body-s-size)',
        fontWeight: 'var(--body-s-weight)',
        color: 'var(--text-secondary)',
        lineHeight: '1.4',
      }}>7 border-radius and 4 border-weight tokens for consistent container shaping.</div>
    </div>

    <div style={{
      padding: 'var(--spacing-xl)',
      borderRadius: 'var(--border-radius-s)',
      border: '1px solid var(--border-soft)',
      background: 'var(--background-default)',
    }}>
      <div style={{
        fontSize: '28px',
        marginBottom: 'var(--spacing-xs)',
      }}>☁️</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--label-m-size)',
        fontWeight: 'var(--label-m-weight)',
        color: 'var(--text-primary)',
        marginBottom: 'var(--spacing-2xs)',
      }}>Shadows</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--body-s-size)',
        fontWeight: 'var(--body-s-weight)',
        color: 'var(--text-secondary)',
        lineHeight: '1.4',
      }}>5 elevation levels plus a brand gradient for depth and hierarchy.</div>
    </div>

    <div style={{
      padding: 'var(--spacing-xl)',
      borderRadius: 'var(--border-radius-s)',
      border: '1px solid var(--border-soft)',
      background: 'var(--background-default)',
    }}>
      <div style={{
        fontSize: '28px',
        marginBottom: 'var(--spacing-xs)',
      }}>🧩</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--label-m-size)',
        fontWeight: 'var(--label-m-weight)',
        color: 'var(--text-primary)',
        marginBottom: 'var(--spacing-2xs)',
      }}>Components</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--body-s-size)',
        fontWeight: 'var(--body-s-weight)',
        color: 'var(--text-secondary)',
        lineHeight: '1.4',
      }}>Button, Input Field, Segmented Control — built with semantic tokens only.</div>
    </div>

  </div>
</div>

<div className="docs-section">
  <div className="docs-section-title">Two-Layer Token Model</div>

  <div style={{
    display: 'grid',
    gridTemplateColumns: '1fr 1fr',
    gap: 'var(--spacing-m)',
    marginTop: 'var(--spacing-l)',
    maxWidth: '600px',
  }}>
    <div style={{
      padding: 'var(--spacing-l)',
      borderRadius: 'var(--border-radius-s)',
      background: 'var(--background-canvas)',
    }}>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--label-m-size)',
        fontWeight: 'var(--label-m-weight)',
        color: 'var(--text-primary)',
        marginBottom: 'var(--spacing-xs)',
      }}>Primitive</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--body-s-size)',
        color: 'var(--text-secondary)',
        lineHeight: '1.4',
      }}>Raw values — colors, spacing, radii. Never used directly in components.</div>
      <code style={{ marginTop: 'var(--spacing-xs)', display: 'inline-block' }}>--color-green-600</code>
    </div>
    <div style={{
      padding: 'var(--spacing-l)',
      borderRadius: 'var(--border-radius-s)',
      background: 'var(--background-active)',
      border: '1px solid var(--border-highlight)',
    }}>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--label-m-size)',
        fontWeight: 'var(--label-m-weight)',
        color: 'var(--text-primary)',
        marginBottom: 'var(--spacing-xs)',
      }}>Semantic</div>
      <div style={{
        fontFamily: 'var(--font-family-brand), sans-serif',
        fontSize: 'var(--body-s-size)',
        color: 'var(--text-secondary)',
        lineHeight: '1.4',
      }}>Purpose-driven — describes intent. Components use only these.</div>
      <code style={{ marginTop: 'var(--spacing-xs)', display: 'inline-block' }}>--interactive-primary</code>
    </div>
  </div>
</div>

<div className="docs-section" style={{ marginTop: '48px' }}>
  <div className="docs-section-title">License</div>
  <div
    style={{
      fontFamily: 'var(--font-family-brand), sans-serif',
      fontSize: 'var(--body-s-size)',
      fontWeight: 'var(--body-s-weight)',
      color: 'var(--text-secondary)',
      lineHeight: '1.6',
      marginTop: 'var(--spacing-m)',
    }}
  >
    {'Licensed under the '}
    <a
      href="https://github.com/CKWAG/ckw-elements-design/blob/main/LICENSE"
      style={{ color: 'var(--interactive-primary)' }}
    >
      {'Apache License 2.0'}
    </a>
    {'.'}
  </div>
  <div
    style={{
      fontFamily: 'var(--font-family-brand), sans-serif',
      fontSize: 'var(--caption-size, 12px)',
      color: 'var(--text-tertiary)',
      marginTop: 'var(--spacing-xs)',
      lineHeight: '1.5',
    }}
  >
    {'The CKW logo and Gotham typeface are excluded from the open-source license. See '}
    <a
      href="https://github.com/CKWAG/ckw-elements-design/blob/main/NOTICE"
      style={{ color: 'var(--interactive-primary)' }}
    >
      {'NOTICE'}
    </a>
    {' for details.'}
  </div>
  <div
    style={{
      marginTop: 'var(--spacing-m)',
      fontFamily: 'var(--font-family-brand), sans-serif',
      fontSize: 'var(--body-s-size)',
      color: 'var(--text-secondary)',
    }}
  >
    <a
      href="https://github.com/CKWAG/ckw-elements-design"
      style={{ color: 'var(--interactive-primary)', textDecoration: 'none' }}
    >
      {'GitHub'}
    </a>
    {' · '}
    <a
      href="https://www.ckw.ch"
      style={{ color: 'var(--interactive-primary)', textDecoration: 'none' }}
    >
      {'ckw.ch'}
    </a>
  </div>
</div>

<div
  style={{
    marginTop: 'var(--spacing-xl)',
    paddingTop: 'var(--spacing-m)',
    borderTop: '1px solid var(--border-soft)',
    fontFamily: 'var(--font-family-brand), sans-serif',
    fontSize: 'var(--caption-size, 12px)',
    color: 'var(--text-tertiary)',
    textAlign: 'center',
  }}
>
  {'© 2026 CKW AG. All rights reserved.'}
</div>

</div>
